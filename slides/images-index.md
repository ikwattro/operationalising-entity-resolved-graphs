# Images Index

| Slide | Description |
|---|---|
| 9 (Globalisation) | Side-by-side comparison of the same entity name in Arabic script and English transliteration, showing how the same person appears across two sources |
| 12 (End-to-End Flow) | Architecture diagram: source graph nodes → record extraction → Senzing engine → affected entity output → graph write-back overlay |
| 17 (Storing Resolved Entities) | Graph schema diagram showing source nodes (e.g. Person, Organisation) linked to a ResolvedEntity node via BELONGS_TO relationships, with metadata on the edges |
